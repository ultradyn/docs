# Small labelled ingestion corpus

Fixture provenance: this corpus is a curated, shortened, stable subset adapted from `CONTEXT.md`, specifically `Knowledge flow`, `Automatic ingestion`, `State and priority`, and `Local boundaries`. It is deliberately copied into fixture-owned source files: tests do not read live repository documentation at runtime. Exact source paths and sections are recorded in `expected-graph.json`.

The labels are hand-authored evaluation truth. Paths and source bytes are fixed by literal SHA-256 digests in `expected-graph.json`.
