{
  "type": "object",
  "additionalProperties": false,
  "required": ["decision", "matchedQuestionId", "rationale", "confidence"],
  "properties": {
    "decision": {
      "enum": ["active", "deferred", "answered", "novel"]
    },
    "matchedQuestionId": {
      "type": ["string", "null"]
    },
    "rationale": {
      "type": "string"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    }
  }
}
